*.iml
.gradle/
build/
local.properties
.idea/
.DS_Store
captures/
.externalNativeBuild
.cxx

# Release signing. Losing the keystore means losing the ability to update the app
# on Play, so keep a backup somewhere safe — just never in this repo.
keystore.properties
*.jks
*.keystore

# Local-only working files, e.g. Play store listing assets.
/local/
